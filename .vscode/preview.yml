apps:
  - port: 3000
    run: npm install && npm run build && npm run start -- --hostname 0.0.0.0 --port 3000
    root: ./
    name: Chen Personal OS
    autoOpen: true
    autoPreview: true
    mainPort: true
